# Dependencies
node_modules/
vendor/

# Build artefacts produced by tooling not yet committed
# (build/ from @wordpress/scripts is intentionally NOT ignored — it is committed)
*.log
.phpunit.result.cache
.phpunit.cache/
coverage/
.npm/

# Release artefact emitted by ./build-release-zip.sh — uploaded to the
# GitHub release as an asset, not committed.
/kntnt-gpx-blocks.zip

# Editor / OS files
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local AI-agent config (Claude Code etc.) — per-developer, not project content
.claude/

# Local config
.env
.env.local

# Composer / npm lock files are kept (committed)
